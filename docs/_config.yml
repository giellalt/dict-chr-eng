theme: jekyll-theme-giellalt
title: Dictionary for Cherokee ⇒ English
description: An electronic dictionary from Cherokee to English

defaults:
  - scope: { path: "" }
    values:
      layout: default

markdown: kramdown
kramdown:
  syntax_highlighter_opts:
    disable: true
